## need median rank function


## need region annoation function
  # allow to specify certain node in graph
  # include only top n median of voxels of substructures

## need function to reduce matrix to specific annotation level
  # graph handling
  # take target level


## need function that returns likely and other clusters as data.frame

## need wrapper functio(s)
  # execute above pipeline for sets of marker genes and store (intermediate) results
  # need to allow confidence measure based on
